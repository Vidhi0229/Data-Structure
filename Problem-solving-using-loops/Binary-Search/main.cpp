#include<iostream>
#include<bits/stdc++.h>
using namespace std;


int main(){
    int n, num, key;
    vector <int> Num;
    cout << "Enter size of array: ";
    cin >> n;
    cout << "Enter Number in ascending order\n";
    for(int i = 0; i < n; i++){
        cin >> num;
        Num.push_back(num);
    }

    if(key < Num[n/2]){
        for(int i = 0; i < n/2; i++){
            
        }
    }
}